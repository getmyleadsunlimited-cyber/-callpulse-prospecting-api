import { EmptyState } from "@/components/states";
export default function HomePage() { return <EmptyState title="Your recovery workspace is ready" description="Dashboard and lead inbox experiences will be added in subsequent product branches." />; }
