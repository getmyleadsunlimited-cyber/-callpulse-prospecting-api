# Server-only. Never prefix backend URLs or credentials with NEXT_PUBLIC_.
CALLPULSE_API_URL=http://127.0.0.1:8000
# Exact public origin used by BFF mutation-origin validation.
# Browsers treat localhost as a secure context; deployed environments must use HTTPS.
APP_ORIGIN=http://localhost:3000
